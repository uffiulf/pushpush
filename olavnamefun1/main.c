#include <stdlib.h>
#include <stdio.h>
#include "ctype.h"
#include <string.h>



// Ask name of user
// If input is a number,  Run printf "Use letters"
//when input with letters, Run printf "Hi, nice to meet you "letters"

int main(void){
// prøver å lage et enkelt regnestykke
    int x = scanf("x: ");
    int y = scanf("y: ");

    //printf("%i\n", x+y);


    int = result(int userinput)
    {
        return userinput;
    }

    printf("your number here: %i,\n", result);
    scanf("%i", );
    int userinput = result(number)



    //string answer = get_string("What is your name?");  // need header cs50 for this to work
   // char *answer;

  //  if
    
        // if input is a number, run printf "Use letters"
    //    answer == < 0;

   // else


  //  printf("Hi, nice too meet you %s", answer);
//    scanf("%s");


            return 0;
}